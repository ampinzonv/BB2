   function warning () {
    # ------------------------------------
    # Same as `error`, but printed in blue
    # ------------------------------------

      error "${ANSI_BLUE}$*${ANSI_RESET}"
   }